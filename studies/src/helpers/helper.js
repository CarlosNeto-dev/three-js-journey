import * as THREE from 'three';

// =========================================== Colocando os eixos X, Y e Z e o Grid ===========================================

export const axes = new THREE.AxesHelper(5);
export const grid = new THREE.GridHelper(10, 10);
